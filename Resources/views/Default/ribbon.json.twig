{
   "items":
   [
      {   "type":"block", 
          text:"{{ "module.Doc" | trans }}", 
          "text_pos": "top", 
          "list":
          [
             {   "type": "button", 
                 text:"", 
                 id: "menu_home",
                 "isbig": true, 
                 "img": "48/doc.png"
             },
             {   "type": "button", 
                 text:"", 
                 id: "menu_note",
                 "img": "note.png"
             }                           
             {% if Folder is not empty %}
             ,
             {   "type": "buttonSelect", 
                 text:"", 
                 "img": "folder_page.png", 
                 items:
                 [ 
                    {% for file in Folder %} 
                    { id: "{{ file.id }}", text: "{{ file.name }}"},
                    {% endfor %}            
                 ]
              }
              {% endif %}
          ]
      }
   ]
}
